/**
 * @fileoverview 外部プラグインなどの圧縮を回避するためのファイル
 */

/**
 * @type {Object}
 */
document['wacom-plugin'] = {};

/**
 * @type {boolean}
 */
document['wacom-plugin'].isWacom;

/**
 * @type {number}
 */
document['wacom-plugin'].pointerType;

/**
 * @type {number}
 */
document['wacom-plugin'].pressure;

